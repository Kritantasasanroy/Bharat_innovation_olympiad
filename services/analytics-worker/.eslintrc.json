{
	"root": true,
	"parser": "@typescript-eslint/parser",
	"parserOptions": {
		"ecmaVersion": "latest",
		"sourceType": "module"
	},
	"rules": {
		"no-restricted-imports": [
			"error",
			{
				"patterns": [
					{
						"group": ["@adapters/*", "@adapters/**/*"],
						"message": "Core layer must not import from adapters. Define a port interface instead."
					},
					{
						"group": ["@infra/*", "@infra/**/*"],
						"message": "Core layer must not import from infrastructure. Define a port interface instead."
					},
					{
						"group": ["**/adapters/**"],
						"message": "Core layer must not import from adapters via relative paths. Define a port interface instead."
					},
					{
						"group": ["**/infra/**"],
						"message": "Core layer must not import from infrastructure via relative paths. Define a port interface instead."
					},
					{
						"group": [
							"@bio/admin-shared-types",
							"@bio/admin-shared-types/*",
							"@bio/admin-contract-fixtures",
							"@bio/admin-contract-fixtures/*",
							"@bio/shared-types",
							"@bio/shared-types/*",
							"@bio/domain-contracts",
							"@bio/domain-contracts/*"
						],
						"message": "Import cross-repo contracts only through the seam at @contracts (src/contracts). Do not import the @bio/* contract packages directly, and do not redeclare their DTOs."
					}
				]
			}
		]
	},
	"overrides": [
		{
			"files": ["src/contracts/**/*.ts"],
			"rules": {
				"no-restricted-imports": "off"
			}
		}
	]
}
