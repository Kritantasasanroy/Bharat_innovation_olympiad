import { describe, expect, it } from "bun:test";

describe("portal contract fixtures", () => {
	it("names RegistrationConfirmed", () => {
		expect("RegistrationConfirmed").toBe("RegistrationConfirmed");
	});
});
