// Reserved module home for the results domain.
// Public surface is barrel-only: export every consumer-facing symbol from here.
// Domain code must not import from adapter or infra layers (see .eslintrc.json
// and test/boundary.test.ts).
export {};
